# Railway Nixpacks config â€” alternative to Dockerfile
# Railway will auto-detect Python and use this

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = [
  "mkdir -p static templates uploads",
  "echo ''Build complete â€” static and templates dirs created''"
]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"

